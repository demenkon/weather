import React from "react";

const Info = () => (
   <div>
      <h2 className="infoTitle">Weather</h2>
      <p className="infoText">Find out the weather in your city</p>
   </div>
)

export default Info;